import * as NavigationBar from 'expo-navigation-bar';

function inicio() {
    const visibility = NavigationBar.useVisibility()

}

const color = await NavigationBar.getBackgroundColorAsync(black);
await NavigationBar.getBehaviorAsync()
